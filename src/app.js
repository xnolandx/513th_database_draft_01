/* 
workshop API requirements:
- must contain endpoints that cover all CRUD functionality
- must contain parameterized endpoints or endpoints that handle query parameters
*/

const express = require('express');
const app = express();
const port = 8081;